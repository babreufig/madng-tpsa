# Libraries advertised by Xgtpsa to Xobjects

This folder will be automatically populated by the build process (`pip install -e path/to/xgtpsa`)
and will contain the GTPSA library (`libmadng_tpsa.{dylib,so}`).
