# Include files advertised by Xgtpsa to Xobjects

This folder will be automatically populated by the build process (`pip install -e path/to/xgtpsa`)
and will contain the patched GTPSA public headers from MAD-NG.
